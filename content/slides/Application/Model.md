### Define a model to store the weather. ###

``` javascript
var WeatherModel = Backbone.Model.extend({
  url: function() {},

  parse: function(resp) {}
});
```

<style scoped>
  @host {
    background: #FFF;
  }
</style>
